services:
  db:
    image: postgres:16
    container_name: post-panel-db
    env_file:
      - .env.local
    ports:
      - "5434:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata: