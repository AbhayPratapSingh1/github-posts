meta {
  name: Auth - Login
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/api/auth/login
  body: json
  auth: none
}

body:json {
  {
    "userid": "admin",
    "password": "admin_password"
  }
}

assert {
  res.status: eq 200
  res.body.success: eq true
}
