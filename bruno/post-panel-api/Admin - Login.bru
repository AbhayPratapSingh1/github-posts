meta {
  name: Admin - Login
  type: http
  seq: 7
}

post {
  url: {{baseUrl}}/api/admin/login
  body: json
  auth: bearer
}

body:json {
  {
    "password": "admin_password"
  }
}

assert {
  res.status: eq 200
  res.body.success: eq true
}
