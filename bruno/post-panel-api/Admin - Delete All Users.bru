meta {
  name: Admin - Delete All Users
  type: http
  seq: 12
}

delete {
  url: {{baseUrl}}/api/admin/users
  body: none
  auth: bearer
}

assert {
  res.status: eq 200
  res.body.message: contains "Deleted"
}
