# Pending Changes

Small changes to batch into a single commit later.

- [ ] Remove back icon (`FaArrowLeft`) from Post page navbar (before logo)
- [ ] Remove "Back to top" link from Post page footer
- [ ] Remove credentials login (User ID/Password input box) from Login page — keep only GitHub login
